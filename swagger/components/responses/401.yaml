description: Unauthorized error
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - data
      properties:
        status:
          type: integer
          example: 401
        name:
          type: string
          example: "AnauthorizedError"
        data:
          type: object
          properties:
           message:
            type: string
            example: 'Unauthorized'
